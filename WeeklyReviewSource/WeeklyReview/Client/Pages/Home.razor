@page "/home"
@* <PageTitle>Home</PageTitle>

<h1>Welcome</h1>

<p>
    This website is currently under active development by Theodor Risager, but you are of course still more than welcome to look around.
</p>

<p>
    If you are not logged in, then some randomly generated data will be used to showcase the different featueres of the app.
</p>

<p>
    You can check out the state of development over at <a href="https://github.com/theodor349/WeeklyReview">GitHub</a>.
</p>

<Syncfusion.Blazor.Inputs.SfColorPicker></Syncfusion.Blazor.Inputs.SfColorPicker> *@

@using Syncfusion.Blazor.Inputs
<div class="control-section">
    <div id="inline-control">
        <div class="row">
            <div id="preview" style="@colorValue"></div>
        </div>
        <div id="inline-content" class="row">
            <div class="col-xs-12 col-sm-12 col-lg-6 col-md-6">
                <h4>Inline Palette</h4>
                <SfColorPicker Mode="ColorPickerMode.Palette" ModeSwitcher="false" Inline="false" ShowButtons="false" ValueChange="@onChange"></SfColorPicker>
            </div>
            <div class="col-xs-12 col-sm-12 col-lg-6 col-md-6">
                <h4>Inline Picker</h4>
                <SfColorPicker ModeSwitcher="false" ShowButtons="false" ValueChange="@onChange"></SfColorPicker>
            </div>
        </div>
    </div>
</div>
@code {
    private string colorValue = "background-color: #008000";
    public void onChange(ColorPickerEventArgs args)
    {
        colorValue = "background-color: " + args.CurrentValue.Hex;
    }
}
<style>
    /* Common sample level styles */
    #inline-control {
        width: 100%;
        text-align: center;
    }
    #inline-control .col-xs-12 {
        margin: 10px 0;
    }
    #inline-control h4 {
        margin-bottom: 20px;
    }
    .mobile #inline-control h4 {
        font-size: 20px;
    }
    /* Common sample level styles for mobile */
    .mobile #inline-control h4 {
        margin-bottom: 15px;
    }
    .mobile #inline-control #inline-content {
        display: flex;
        flex-wrap: nowrap;
        overflow-x: auto;
    }
    .mobile #inline-control #inline-content .col-xs-12 {
        flex: 0 0 auto;
        margin-top: 5px;
    }
</style>